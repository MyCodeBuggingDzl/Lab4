Part B

The implementations executes because of the polymorphism.
The variable are declared as Animal, and Java checks the object type at execution.

Part C

The array list can hold dog, cat & frogs because it's a "is-a" relationship.

Part D

animal.speak works because the reference type does not declare fetch, only speak.

Part E

1. 1 Object.
2. Animal
3. dog
4. Yes

In (Animal) dog, the cats is unnecessary because dog is a subclass of animal

Part F

1. Since the object was a subclass.
2. No.
3. dog.

Part G

instanceof checks the object type and not the reference type. If the object matches the class, it returns true;

Part H

It will be a ClassCastException. The compiler only checks the references type Animal. While
Java check the objects type. In this case, dog is a subclass of Animal so u can downcast.
However the object type is a Cat instead of a Dog.
